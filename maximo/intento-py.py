a = lista[-1,0,-1]
		b = lista[a]
		if lista[b] > lista[b+1]:
				c= lista[b]
				lista[b] = lista[b+1]
				lista[b+1] = c
	return lista			

	if len(lista) == 1:
		return lista[0]
	else:
		lista[0:] < lista[1:]+1 == lista[1]	

#Creo que con ajustes servira
		if lista[0] >= lista[0:]:
		return lista[0]
	else:
		lista[0] < lista[1:]
		return maximo(lista[0]+1)


#si sirve 
if lista[0] >= lista[0:]:
		return lista[0]
	else:
		lista[0] < lista[1:]
		return maximo(lista[0]+1)	



	if lista[0:] > lista[1:]:
		return lista[0]
	else:
		lista[0] <= lista[1]
		return maximo(lista[1:])			